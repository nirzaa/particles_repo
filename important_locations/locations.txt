base/base_trainer.py:
120 : defining when to save checkpoint

analyze_auto.py
36 : when to calculate the loss of the epochs
also change at the main function

# Changing number of classes:
train.py : 76
config.json : change there the number of classes + model type
utils/my_utils.py : 70 : bars = np.linspace(1, 13, bin_num)

# noise:
data_loader/data_loaders.py
184: d_tens += en_dep_noise

# physical layers:
data_loader/data_loaders.py
258 : return d_tens.sum(axis=2)[:,:,:], final_list, num_showers, idx

Define the bins:
data_loader/data_loaders.py
245 : bin_list = np.linspace(1, 13, bin_num)

# saving csv:
test.py
116 : df.to_csv(os.path.join(my_path, "data_frame.csv"))

utils/my_utils.py
152:
np.savetxt(os.path.join(csv_path, 'hist_output.csv'), output, delimiter=',')
np.savetxt(os.path.join(csv_path, 'hist_target.csv'), target, delimiter=',')


sum_pixels:
data_loader/data_loaders.py
269
train.py : 18

Generate new data every run:
run_auto.py : 37

# ==== Change number of layers ==== #
data_loader/data_loaders.py : 269 : return d_tens.sum(axis=2)[:,:,:10], final_list, num_showers, idx
config.json : "type": "model_2d_60_10",
model/model.py : model_2d_60_10 : put breakpoint there and run train.py under debugging to see that the dimensions are correct in the model (make sure that the architecture is similar to last run)

# ==== After finishes ==== #
saved_models: move into new folder
csv_files: move into folder
tmux-save-pane > ./csv_files/terminal_tmux.txt

mv ./csv_files/run_* ./csv_files/multiple_runs/case_2/
mv ./csv_files/terminal* ./csv_files/multiple_runs/case_2/                                         
mv ./saved/models/saved_models/run_* ./saved/models/saved_models/multiple_runs/case_2/

analyze_auto.py:
37 : adjust the line
105, 106 : adjust the line


